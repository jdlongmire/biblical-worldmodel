# Repository foundation and bridge transfer

**WP-BWM-0004 — implemented and verified; JD acceptance pending.**

JD directed the repository to be created and structured from template first, then the associated work packages to move here. This package delivers that foundation only. WP-BWM-0001, 0002 and 0003 remain unfinished delivery work.

The public repository was generated using GitHub's template operation from `jdlongmire/longmire-repo-template`. Its history is merged with the earlier local WP-BWM-0001 planning commits, preserving both histories. The existing planning branch/worktree is reused for this bounded foundation slice; its branch name is not evidence that the site implementation is complete.

The source handoff comprises four files: publication requirements, visual requirements, asset manifest, and community/contact requirements. `migration.json` retains exact source revision and source/target digests. Relative parent links are retargeted where needed; imported status and role statements describe the source requirements, not current implementation or additional authorization.

The template class is deliberately specialized by ADR-BWM-0001. No site, hosted workflow, form, discussion service, narration or Home runtime is deployed by this package. The destination import commit was published and verified before the bridge files became forwarding records. See [evidence](evidence.md) and [publication evidence](publication-evidence.json).

Human-Curated, AI-Enabled (HCAE)
