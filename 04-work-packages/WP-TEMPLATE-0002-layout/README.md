# Align template with thinx root

The previous starter differed from thinx at the root and MxM paths. This package
adopts the current structure and adds regression checks for the actual adapter
chain and canonical surface locations. Runtime services remain separately scoped.

See [package.yaml](package.yaml) for authority and verification.

Verification: [layout evidence](../../02-systems-baseline/2.5-verification/layout-evidence.md).
