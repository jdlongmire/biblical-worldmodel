# Work model

The operator directs and accepts; the aide executes and reports evidence.
A work package belongs to one repository and declares intent, scope, authority,
criteria and disposition. A WorkUnit is bounded intent; a Run is one actual
execution attempt. Verification records artifact, checks, environment and result.
Human acceptance and runtime activation remain separate from passing checks.
A branch or worktree is isolation, not proof that a process is running.
