# ADR-BWM-0001: Publication product profile from the repository template

Date: 2026-09-11. Status: implemented specialization under JD's direction to establish this product from template.

The current template defaults to a personal Meta-Harness. Biblical WorldModel is instead a publication product. Retain the template's 00–06 layout, six working-guidance surfaces, adapters, memory discipline and explicit lifecycle checks. Declare General Work / Publication and specialize both structural checkers accordingly. The profile names existing thinx stewardship, not an additional assistant identity. This deliberately replaces the template-class default for this repository only; historical template ADRs document their original context.

Future MkDocs implementation lives under `03-solutions-baseline/site/` (including docs, assets and configuration), while GitHub workflows live under `.github/workflows/`. This preserves the template's closed root. The bridge's proposed root-level site tree is a conceptual structure, adjusted here to the selected template.

No runtime, service, bot, identity provider, private memory or credentials are inherited from ThinxHome. Narration production and contact-provider activation are later scoped work. This package only establishes the repository and transfers the backlog; the site, Discussions, forms and deployment workflow are not activated.

Human-Curated, AI-Enabled (HCAE)
