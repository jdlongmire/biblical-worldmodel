# Optional reference profiles

The default implemented profile is the thinx Meta-Harness layout. The retained
[research programme](research-programme.md) is an optional historical reference,
not an automatic change to this repo's profile. Any adoption must reconcile its
paths and authority with [the current layout](../ADR-LAYOUT-0002-thinx-root-parity.md).
