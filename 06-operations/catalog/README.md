# Capability inventory

Git and Python are prerequisites for local session tools. Codex requires the
operator's own installation and account. Record target machine, OS, interface,
verified tools, owner and verification date after onboarding. No remote hosts,
mail accounts, console service or desktop tools are configured by default.
